# Public automatic HTTPS is the production default; test TLS lives outside this file.
{$PUBLIC_HOST} {
	# Caddy immediately flushes text/event-stream automatically; retain normal disconnect handling.
	reverse_proxy wayfarer:8080
}
