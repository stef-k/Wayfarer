[Definition]
# Generic 404 storm detector (tune maxretry in the jail)
failregex = ^<HOST> - - \[.*\] ".*" 404 \d+
ignoreregex = ^<HOST> - - \[.*\] "GET /favicon\.ico HTTP/.*" 404 \d+$
^<HOST> - - \[.*\] "GET /robots\.txt HTTP/.*" 404 \d+$
