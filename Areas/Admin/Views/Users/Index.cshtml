@model IEnumerable<Wayfarer.Models.ViewModels.UserViewModel>

@{
    ViewData["Title"] = "Manage Users";
}

<h1 class="text-center mb-4">@ViewData["Title"]</h1>

<div class="container">
    <div class="row mb-4">
        <div class="col-md-6">
            <a asp-action="Create" asp-controller="Users" class="btn btn-success">Create New User</a>
        </div>
        <div class="col-md-6">
            <form method="get" asp-action="Index" class="d-flex">
                <input type="text" name="search" value="@ViewBag.Search" placeholder="Search users..." class="form-control me-2" />
                <button type="submit" class="btn btn-primary">Search</button>
            </form>
        </div>
    </div>

    <div class="table-responsive">
        <table class="table table-striped table-hover align-middle">
            <thead class="table-light">
                <tr>
                    <th>Username</th>
                    <th>Display Name</th>
                    <th>
                        Role
                        <i class="bi bi-question-circle text-muted ms-1"
                           data-tippy-content="Admin: full system access. Manager: can manage groups and invitations. User: standard access to personal features."></i>
                    </th>
                    <th>
                        Status
                        <i class="bi bi-question-circle text-muted ms-1"
                           data-tippy-content="Inactive users cannot log in. This allows temporarily disabling access without deleting user data."></i>
                    </th>
                    <th>
                        Protected
                        <i class="bi bi-question-circle text-muted ms-1"
                           data-tippy-content="Protected users cannot be deleted. The first admin account is protected by default."></i>
                    </th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody class="table-group-divider">
                @if (!Model.Any())
                {
                    <tr>
                        <td colspan="6" class="text-center text-muted">No users found.</td>
                    </tr>
                }
                else
                {
                    @foreach (var user in Model)
                    {
                        <tr>
                            <td>@user.Username</td>
                            <td>@user.DisplayName</td>
                            <td>@user.Role</td>
                            <td>
                                @if(user.IsActive)
                                {
                                    <span class="badge text-bg-success">
                                        Active
                                    </span>
                                }
                                else
                                {
                                    <span class="badge text-bg-danger">
                                        Inactive
                                    </span>
                                }
                            </td>
                            <td>
                                @if (user.IsProtected)
                                {
                                    <span class="badge text-bg-success">Protected</span>
                                }
                            </td>
                            <td>
                                <div class="btn-group" role="group">
                                    <a asp-action="Edit" asp-route-id="@user.Id" class="btn btn-primary btn-sm">Edit</a>

                                    @if (user.IsCurrentUser)
                                    {
                                        <a href="/Identity/Account/Manage/ChangePassword" class="btn btn-secondary btn-sm">Change Password</a>
                                    }
                                    else
                                    {
                                        <a asp-action="ChangePassword" asp-controller="Users" asp-route-id="@user.Id" class="btn btn-secondary btn-sm">Change Password</a>
                                    }

                                    <a asp-area="Admin" asp-controller="ApiToken" asp-action="Index" asp-route-id="@user.Id" class="btn btn-info btn-sm">Manage Tokens</a>

                                    @if (user.IsProtected)
                                    {
                                        <button class="btn btn-danger btn-sm disabled" title="This user is protected and cannot be deleted.">Delete</button>
                                    }
                                    else
                                    {
                                        <a asp-action="Delete" asp-route-id="@user.Id" class="btn btn-danger btn-sm">Delete</a>
                                    }
                                </div>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>

    <nav aria-label="Page navigation">
        <ul class="pagination justify-content-center">
            @for (int i = 1; i <= ViewBag.TotalPages; i++)
            {
                <li class="page-item @(i == ViewBag.CurrentPage ? "active" : "")">
                    <a class="page-link"
                       asp-area="Admin"
                       asp-action="Index"
                       asp-controller="Users"
                       asp-route-page="@i"
                       asp-route-search="@ViewBag.Search">@i</a>
                </li>
            }
        </ul>
    </nav>
</div>
